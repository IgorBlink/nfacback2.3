# Production requirements для Railway
# Базовые инструменты
setuptools>=68.0.0
wheel>=0.40.0

# Основные зависимости - все стабильные
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
google-generativeai==0.3.2
speechrecognition==3.10.0
pydub==0.25.1
gtts==2.4.0
numpy==1.24.3
python-multipart==0.0.6
jinja2==3.1.2
aiofiles==23.2.1

# Аудио обработка (только если получится установить)
# webrtcvad==2.0.10  # Закомментировано - может не работать в контейнере
# pyaudio==0.2.11   # Закомментировано - для продакшена не нужен (веб-аудио) 